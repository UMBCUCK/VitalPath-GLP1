"use client";
export function ReconciliationClient(props: Record<string, unknown>) {
  return (<div className="space-y-6"><h2 className="text-2xl font-bold text-navy">Payment Reconciliation</h2><p className="text-sm text-graphite-400">Stripe vs database reconciliation and discrepancy tracking.</p></div>);
}
